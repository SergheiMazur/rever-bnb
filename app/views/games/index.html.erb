<%  @games.each do |game|%>
<%= game.name %> <%= link_to "Show Details", game_path(game) %>
<%= game.rating %> <%= link_to "Show Details", game_path(game) %>
<hr>
<%= game.description %> <%= link_to "Show Details", game_path(game) %>
<% end %>
